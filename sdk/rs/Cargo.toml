[package]
name = "tenon-sdk"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
description = "Tenon wire v1.1 SDK: external plugins for the tenon atom kernel"
license = "MIT"

[dependencies]
serde_json = "1"

[[bin]]
name = "example"
path = "src/bin/example.rs"
